# 安全策略

## 报告问题

请通过 GitHub Security Advisory 私下报告可能导致凭证泄露、身份冒用或未授权请求的问题，不要先建立公开 Issue。

报告前请删除或替换以下内容：

- Token、Cookie、二维码内容与完整请求头
- 账号 ID、角色 ID、手机号和邮箱
- `device_id`、`device_fp` 等设备标识
- 含有上述信息的截图和日志

普通兼容性问题可以使用 Issue，但只应提供应用版本、Android 版本、游戏、经过脱敏的错误码和复现步骤。

## 支持范围

安全修复只保证合入当前最新版本。第三方平台要求人工验证时，应用会停止后续请求；不接受破解或绕过验证机制的功能请求。
